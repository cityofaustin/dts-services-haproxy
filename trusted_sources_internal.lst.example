# Developers and city network — can reach all backends.
# Copy to trusted_sources_internal.lst and replace with real values:
#   cp trusted_sources_internal.lst.example trusted_sources_internal.lst
#
# One IP or CIDR per line. Lines starting with # are ignored.
#
# Keep in sync with 1Password Developer vault entry:
#   "Bastion HAProxy Trusted Sources Internal / Developers"
# Changing this file on the bastion does not update 1Password, and vice versa.

# Developer static IPs
203.0.113.10
203.0.113.11
203.0.113.12

# City / office network (class C example)
162.89.100.0/24
